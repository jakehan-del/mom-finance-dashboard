# 월간 갱신 절차서 — mom-finance-dashboard

> 이 문서만 보고 처음 온 사람(또는 AI)이 갱신을 끝낼 수 있어야 한다.
> 이 프로젝트에 대해 아무것도 모른다고 가정하고 쓴다.

## 이 앱이 무엇인가

- **어머니(인순이)가 쓰시는 재무 대시보드.** GitHub Pages 정적 사이트
  (https://jakehan-del.github.io/mom-finance-dashboard/), 파일은 `index.html` 하나.
- 로그인: 4자리 PIN (클라이언트 측 — 보안장치가 아니라 문턱이다. 소스에 그대로 보인다).
- 모든 숫자가 `index.html` 에 **하드코딩**돼 있다. 자동 동기화가 없으므로
  시트가 바뀌면 **사람이 이 절차로 밀어 넣어야 한다.**
- 서비스워커(`sw.js` v8~)는 **네트워크 먼저**라, 배포하면 어머니 폰에는
  다음 열기 때 저절로 반영된다. 캐시 버전을 올릴 필요 없다.

## 정본이 어디인가

- 스프레드시트 `1p33ArO7ebx5gSWp1Ibq_0m7_z4eqze0X_Rg5cpbncAU`
  - 월별 수입·지출: `소솝_YYYY Summary/Financial`, `개인_YYYY …` 탭
  - 잔고: `잔고내역` 탭 (비고 「대출금」= 대출, 나머지 = 예금·펀드)
  - 미술품: `그림내역법인`, `그림내역개인` (판매액(J열) 있으면 매각 → 보유 제외)
- 읽기는 **Apps Script 웹앱**으로만 한다. URL·토큰은
  `~/Documents/Claude/Projects/어머니 소솝 법인 및 개인 재무 분석/insooni-finance/.env.local`
  의 `SHEETS_API_URL` / `SHEETS_API_TOKEN`. **토큰을 출력·기록하지 말 것.**
- 부동산 240억·전세 45억은 시트에 없다 — 앱에만 있는 값이므로 Jake 에게 확인.

## 절차 (순서 지킬 것)

1. **월별 자료 변화 확인** — insooni-finance 에서 `npm run sync`.
   `unchanged` 라도 잔고·그림 탭은 동기화 대상 밖이므로 2번으로.
2. **자산 탭 읽기**
   ```
   curl -sL "${SHEETS_API_URL}?token=${SHEETS_API_TOKEN}&tabs=잔고내역,그림내역법인,그림내역개인" -o /tmp/tabs.json
   ```
   (탭 이름은 URL 인코딩 필요)
3. **바뀐 것 파악** — 시트의 SUMIF 합계 행(잔고내역 법인 20·21행, 개인 35·36행 부근,
   그림 「보유 합계」 행)을 앱의 현재 값과 비교. 앱 현재 값은 `index.html` 을 grep.
4. **치환 스크립트 작성** — 반드시 이 틀로:
   - 모든 치환은 `assert count == 기대치` — 하나라도 어긋나면 **파일을 저장하지 않는다**
   - 시트 추출 합계를 시트의 SUMIF 값과 assert 로 먼저 대조
   - 마지막에 **낡은 값 잔재 0 확인** (아래 「잘 틀리는 곳」의 문자열 전부)
5. **로컬 검증** — `python3 -m http.server` 로 띄우고:
   잠금 우회(`lock` 숨기고 `rl(); initC()`) 후 새 값·표 2개·차트 11개 전부 그려지는지,
   `<table>`/`<div>` 짝이 맞는지.
6. **배포** — commit(한국어, 무엇을 왜) → `git push origin main` (계정 jakehan-del, gh 인증됨).
7. **라이브 검증** — 40초 뒤 `curl` 로 새 값 grep. 0이면 60초 더 기다렸다 재시도
   (GitHub Pages 전파 지연).

## 잘 틀리는 곳 (전부 실제로 겪은 것)

1. **챗봇 답변과 SYS 프롬프트에 같은 숫자가 서로 다른 형식으로 있다.**
   `예금: 9.3억`(칸 답변)과 `예금9.3억`(SYS, 붙여쓰기) — 한쪽만 고치고 두 달 연속 놓쳤다.
   잔재 검사에 **띄어쓰기 없는 형태**도 넣을 것.
2. **합계가 여러 표기로 흩어져 있다.** 같은 값이 `8.1억`(카드) / `약 8억 622만`(표) /
   `~8.1억`(챗봇) / `[…,11.0,23.2]`(차트 배열)로 존재한다. 하나의 원천값에서 전부 재계산할 것.
3. **기준일 라벨.** 시트의 「기록일」 셀은 사람이 안 고치고 잔고만 고치는 경우가 있다.
   그때 앱에는 **읽은 날짜**(「N월 N일 확인」)로 적는다 — 검증 가능한 유일한 날짜다.
   기록일을 지어내지 말 것.
4. **구글시트를 브라우저로 편집할 때 한글 첫 글자가 먹힌다** (IME 조합).
   셀을 더블클릭해 편집모드로 들어간 뒤 입력할 것. 수식(`=` 시작)은 안전하다.
5. **좌표 클릭 전에 반드시 새 스크린샷.** 시트가 스크롤되면 지난 좌표는 다른 셀이다.
6. Warhol jackie·FLOWER 는 **소송 관련 — 앱에 넣지 않는다** (Jake 지시, 2026-08).
   매각된 작품도 평가에서 제외. 시트의 「보유 합계」 SUMIF 가 자동으로 걸러 준다.

## 하지 않는 것

- 이 앱에서 시트를 **쓰지** 않는다 (읽기만).
- 통화 임의 환산 금지 — 외화는 시트의 환산값·환율만 옮긴다.
- 조용한 실패 금지 — assert 가 터지면 그대로 보고하고 멈춘다.
